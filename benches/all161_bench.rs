//! AUTO-GENERATED by tools/bench/gen_all161.py — do not edit by hand.
//! All 161 adaq-talib indicators benchmarked against native TA-Lib C (via abstract API /
//! direct FFI for candles). Run: cargo bench --bench all161_bench --features bench-c
#![allow(dead_code, unused_imports, non_camel_case_types, non_upper_case_globals)]

#[cfg(feature = "bench-c")]
mod talib_ffi {
    use std::ffi::{c_void, CStr, CString};
    use std::os::raw::{c_char, c_double, c_int, c_uint};

    pub type TA_Integer = c_int;
    pub type TA_Real = c_double;
    pub type TA_RetCode = c_int;

    #[repr(C)]
    pub struct TA_ParamHolder { _priv: *mut c_void }

    #[repr(C)]
    pub struct TA_FuncInfo {
        pub name: *const c_char,
        pub group: *const c_char,
        pub hint: *const c_char,
        pub camel_case_name: *const c_char,
        pub flags: c_int,
        pub nb_input: c_uint,
        pub nb_opt_input: c_uint,
        pub nb_output: c_uint,
        pub handle: *const c_uint,
    }

    #[repr(C)]
    pub struct TA_InputParameterInfo {
        pub type_: c_int,
        pub param_name: *const c_char,
        pub flags: c_int,
    }

    #[repr(C)]
    pub struct TA_OptInputParameterInfo {
        pub type_: c_int,
        pub param_name: *const c_char,
        pub flags: c_int,
        pub display_name: *const c_char,
        pub data_set: *const c_void,
        pub default_value: c_double,
        pub hint: *const c_char,
        pub help_file: *const c_char,
    }

    pub const TA_INPUT_PRICE: c_int = 0;
    pub const TA_INPUT_REAL: c_int = 1;
    pub const TA_OPT_REAL: c_int = 0;
    pub const TA_OPT_REAL_LIST: c_int = 1;

    #[repr(C)]
    pub struct TA_OutputParameterInfo {
        pub type_: c_int,
        pub param_name: *const c_char,
        pub flags: c_int,
        pub display_name: *const c_char,
        pub help_file: *const c_char,
    }
    pub const TA_OUTPUT_REAL: c_int = 0;
    pub const TA_OUTPUT_INTEGER: c_int = 1;

    pub type TA_CallForEachFunc = extern "C" fn(*const TA_FuncInfo, *mut c_void);

    #[allow(non_camel_case_types, dead_code)]
    unsafe extern "C" {
        pub fn TA_Initialize() -> TA_RetCode;
        pub fn TA_Shutdown() -> TA_RetCode;
        pub fn TA_GetFuncHandle(name: *const c_char, handle: *mut *const c_uint) -> TA_RetCode;
        pub fn TA_GetFuncInfo(handle: *const c_uint, info: *mut *const TA_FuncInfo) -> TA_RetCode;
        pub fn TA_ParamHolderAlloc(handle: *const c_uint, params: *mut *mut TA_ParamHolder) -> TA_RetCode;
        pub fn TA_ParamHolderFree(params: *mut TA_ParamHolder) -> TA_RetCode;
        pub fn TA_SetInputParamRealPtr(params: *mut TA_ParamHolder, idx: c_uint, value: *const TA_Real) -> TA_RetCode;
        pub fn TA_SetInputParamPricePtr(params: *mut TA_ParamHolder, idx: c_uint, open: *const TA_Real, high: *const TA_Real, low: *const TA_Real, close: *const TA_Real, volume: *const TA_Real, open_interest: *const TA_Real) -> TA_RetCode;
        pub fn TA_SetOptInputParamInteger(params: *mut TA_ParamHolder, idx: c_uint, value: TA_Integer) -> TA_RetCode;
        pub fn TA_SetOptInputParamReal(params: *mut TA_ParamHolder, idx: c_uint, value: TA_Real) -> TA_RetCode;
        pub fn TA_SetOutputParamRealPtr(params: *mut TA_ParamHolder, idx: c_uint, out: *mut TA_Real) -> TA_RetCode;
        pub fn TA_CallFunc(params: *const TA_ParamHolder, start_idx: TA_Integer, end_idx: TA_Integer, out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer) -> TA_RetCode;
        pub fn TA_GetInputParameterInfo(handle: *const c_uint, idx: c_uint, info: *mut *const TA_InputParameterInfo) -> TA_RetCode;
        pub fn TA_GetOptInputParameterInfo(handle: *const c_uint, idx: c_uint, info: *mut *const TA_OptInputParameterInfo) -> TA_RetCode;
        pub fn TA_GetOutputParameterInfo(handle: *const c_uint, idx: c_uint, info: *mut *const TA_OutputParameterInfo) -> TA_RetCode;
        pub fn TA_SetOutputParamIntegerPtr(params: *mut TA_ParamHolder, idx: c_uint, out: *mut TA_Integer) -> TA_RetCode;
        pub fn TA_CDL2CROWS(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDL3BLACKCROWS(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDL3INSIDE(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDL3LINESTRIKE(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDL3OUTSIDE(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDL3STARSINSOUTH(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDL3WHITESOLDIERS(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLABANDONEDBABY(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLADVANCEBLOCK(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLBELTHOLD(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLBREAKAWAY(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLCLOSINGMARUBOZU(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLCONCEALBABYSWALL(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLCOUNTERATTACK(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLDARKCLOUDCOVER(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLDOJI(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLDOJISTAR(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLDRAGONFLYDOJI(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLENGULFING(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLEVENINGDOJISTAR(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLEVENINGSTAR(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLGAPSIDESIDEWHITE(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLGRAVESTONEDOJI(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLHAMMER(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLHANGINGMAN(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLHARAMI(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLHARAMICROSS(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLHIGHWAVE(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLHIKKAKE(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLHIKKAKEMOD(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLHOMINGPIGEON(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLIDENTICAL3CROWS(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLINNECK(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLINVERTEDHAMMER(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLKICKING(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLKICKINGBYLENGTH(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLLADDERBOTTOM(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLLONGLEGGEDDOJI(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLLONGLINE(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLMARUBOZU(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLMATCHINGLOW(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLMATHOLD(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLMORNINGDOJISTAR(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLMORNINGSTAR(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLONNECK(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLPIERCING(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLRICKSHAWMAN(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLRISEFALL3METHODS(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLSEPARATINGLINES(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLSHOOTINGSTAR(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLSHORTLINE(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLSPINNINGTOP(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLSTALLEDPATTERN(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLSTICKSANDWICH(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLTAKURI(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLTASUKIGAP(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLTHRUSTING(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLTRISTAR(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLUNIQUE3RIVER(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLUPSIDEGAP2CROWS(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
        pub fn TA_CDLXSIDEGAP3METHODS(
            start_idx: TA_Integer, end_idx: TA_Integer,
            in_open: *const TA_Real, in_high: *const TA_Real, in_low: *const TA_Real, in_close: *const TA_Real,
            out_beg_idx: *mut TA_Integer, out_nb_element: *mut TA_Integer, out_real: *mut TA_Real
        ) -> TA_RetCode;
    }

    pub const TA_SUCCESS: TA_RetCode = 0;

    #[derive(Debug, Clone)]
    pub enum OptVal { Int(i32), Real(f64) }
    impl OptVal {
        pub fn as_int(&self) -> i32 {
            match self { OptVal::Int(v) => *v, OptVal::Real(v) => *v as i32 }
        }
        pub fn as_real(&self) -> f64 {
            match self { OptVal::Real(v) => *v, OptVal::Int(v) => *v as f64 }
        }
    }

    /// Returns (group, default opt-in values) for a TA-Lib function name.
    pub fn get_func_meta(name: &str) -> Option<(String, Vec<OptVal>)> {
        unsafe {
            let cname = CString::new(name).ok()?;
            let mut handle: *const c_uint = std::ptr::null();
            if TA_GetFuncHandle(cname.as_ptr(), &mut handle) != TA_SUCCESS { return None; }
            let mut info: *const TA_FuncInfo = std::ptr::null();
            if TA_GetFuncInfo(handle, &mut info) != TA_SUCCESS { return None; }
            let group = CStr::from_ptr((*info).group).to_string_lossy().into_owned();
            let nb_opt = (*info).nb_opt_input as usize;
            let mut ov = Vec::with_capacity(nb_opt);
            for o in 0..nb_opt {
                let mut oi: *const TA_OptInputParameterInfo = std::ptr::null();
                if TA_GetOptInputParameterInfo(handle, o as c_uint, &mut oi) != TA_SUCCESS { return None; }
                let dv = (*oi).default_value;
                if (*oi).type_ == TA_OPT_REAL || (*oi).type_ == TA_OPT_REAL_LIST {
                    ov.push(OptVal::Real(dv));
                } else {
                    ov.push(OptVal::Int(dv as i32));
                }
            }
            Some((group, ov))
        }
    }

    /// Drive a non-candle TA-Lib C function via the abstract API (caller-supplied buffers).
    pub fn c_abstract(
        name: &str, n: usize,
        real0: &[f64], real1: &[f64],
        open: &[f64], high: &[f64], low: &[f64], close: &[f64], vol: &[f64],
        iters: usize, opt_vals: &[OptVal],
    ) -> Option<(f64, f64)> {
        unsafe {
            let cname = CString::new(name).ok()?;
            let mut handle: *const c_uint = std::ptr::null();
            if TA_GetFuncHandle(cname.as_ptr(), &mut handle) != TA_SUCCESS { return None; }
            let mut info: *const TA_FuncInfo = std::ptr::null();
            if TA_GetFuncInfo(handle, &mut info) != TA_SUCCESS { return None; }
            let nb_input = (*info).nb_input as usize;
            let nb_opt = (*info).nb_opt_input as usize;
            let nb_out = (*info).nb_output as usize;
            if nb_opt != opt_vals.len() { return None; }
            let mut ph: *mut TA_ParamHolder = std::ptr::null_mut();
            if TA_ParamHolderAlloc(handle, &mut ph) != TA_SUCCESS { return None; }
            let mut real_idx = 0usize;
            for i in 0..nb_input {
                let mut iinfo: *const TA_InputParameterInfo = std::ptr::null();
                if TA_GetInputParameterInfo(handle, i as c_uint, &mut iinfo) != TA_SUCCESS { TA_ParamHolderFree(ph); return None; }
                if (*iinfo).type_ == TA_INPUT_PRICE {
                    if TA_SetInputParamPricePtr(ph, i as c_uint, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), vol.as_ptr(), std::ptr::null()) != TA_SUCCESS { TA_ParamHolderFree(ph); return None; }
                } else {
                    let arr = if real_idx == 0 { real0 } else { real1 };
                    real_idx += 1;
                    if TA_SetInputParamRealPtr(ph, i as c_uint, arr.as_ptr()) != TA_SUCCESS { TA_ParamHolderFree(ph); return None; }
                }
            }
            for o in 0..nb_opt {
                let dv = &opt_vals[o];
                if matches!(dv, OptVal::Real(_)) {
                    if TA_SetOptInputParamReal(ph, o as c_uint, dv.as_real()) != TA_SUCCESS { TA_ParamHolderFree(ph); return None; }
                } else {
                    if TA_SetOptInputParamInteger(ph, o as c_uint, dv.as_int()) != TA_SUCCESS { TA_ParamHolderFree(ph); return None; }
                }
            }
            let mut out_real: Vec<Vec<f64>> = Vec::with_capacity(nb_out);
            let mut out_int: Vec<Vec<i32>> = Vec::with_capacity(nb_out);
            for o in 0..nb_out {
                let mut oi: *const TA_OutputParameterInfo = std::ptr::null();
                if TA_GetOutputParameterInfo(handle, o as c_uint, &mut oi) != TA_SUCCESS { TA_ParamHolderFree(ph); return None; }
                if (*oi).type_ == TA_OUTPUT_INTEGER {
                    let mut buf = vec![0i32; n];
                    if TA_SetOutputParamIntegerPtr(ph, o as c_uint, buf.as_mut_ptr()) != TA_SUCCESS { TA_ParamHolderFree(ph); return None; }
                    out_int.push(buf);
                } else {
                    let mut buf = vec![0.0f64; n];
                    if TA_SetOutputParamRealPtr(ph, o as c_uint, buf.as_mut_ptr()) != TA_SUCCESS { TA_ParamHolderFree(ph); return None; }
                    out_real.push(buf);
                }
            }
            let start = std::time::Instant::now();
            let mut checksum = 0.0f64;
            let mut beg: TA_Integer = 0;
            let mut nb: TA_Integer = 0;
            for _ in 0..iters {
                if TA_CallFunc(ph, 0, n as TA_Integer - 1, &mut beg, &mut nb) != TA_SUCCESS { TA_ParamHolderFree(ph); return None; }
                for o in 0..out_real.len() { checksum += out_real[o][(nb - 1) as usize]; }
                for o in 0..out_int.len() { checksum += out_int[o][(nb - 1) as usize] as f64; }
            }
            let elapsed = start.elapsed();
            TA_ParamHolderFree(ph);
            let ns_per_elem = elapsed.as_nanos() as f64 / (iters as f64 * n as f64);
            Some((ns_per_elem, checksum))
        }
    }

    /// Drive a candle (CDL*) TA-Lib C function via direct FFI (abstract API has a candle quirk).
    pub fn c_candle(name: &str, n: usize, open: &[f64], high: &[f64], low: &[f64], close: &[f64], iters: usize) -> Option<(f64, f64)> {
        unsafe {
            let mut out = vec![0.0f64; n];
            let mut beg: TA_Integer = 0;
            let mut nb: TA_Integer = 0;
            let start = std::time::Instant::now();
            let mut checksum = 0.0f64;
            for _ in 0..iters {
                let rc = match name {
                "CDL2CROWS" => TA_CDL2CROWS(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDL3BLACKCROWS" => TA_CDL3BLACKCROWS(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDL3INSIDE" => TA_CDL3INSIDE(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDL3LINESTRIKE" => TA_CDL3LINESTRIKE(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDL3OUTSIDE" => TA_CDL3OUTSIDE(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDL3STARSINSOUTH" => TA_CDL3STARSINSOUTH(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDL3WHITESOLDIERS" => TA_CDL3WHITESOLDIERS(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLABANDONEDBABY" => TA_CDLABANDONEDBABY(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLADVANCEBLOCK" => TA_CDLADVANCEBLOCK(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLBELTHOLD" => TA_CDLBELTHOLD(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLBREAKAWAY" => TA_CDLBREAKAWAY(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLCLOSINGMARUBOZU" => TA_CDLCLOSINGMARUBOZU(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLCONCEALBABYSWALL" => TA_CDLCONCEALBABYSWALL(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLCOUNTERATTACK" => TA_CDLCOUNTERATTACK(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLDARKCLOUDCOVER" => TA_CDLDARKCLOUDCOVER(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLDOJI" => TA_CDLDOJI(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLDOJISTAR" => TA_CDLDOJISTAR(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLDRAGONFLYDOJI" => TA_CDLDRAGONFLYDOJI(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLENGULFING" => TA_CDLENGULFING(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLEVENINGDOJISTAR" => TA_CDLEVENINGDOJISTAR(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLEVENINGSTAR" => TA_CDLEVENINGSTAR(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLGAPSIDESIDEWHITE" => TA_CDLGAPSIDESIDEWHITE(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLGRAVESTONEDOJI" => TA_CDLGRAVESTONEDOJI(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLHAMMER" => TA_CDLHAMMER(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLHANGINGMAN" => TA_CDLHANGINGMAN(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLHARAMI" => TA_CDLHARAMI(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLHARAMICROSS" => TA_CDLHARAMICROSS(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLHIGHWAVE" => TA_CDLHIGHWAVE(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLHIKKAKE" => TA_CDLHIKKAKE(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLHIKKAKEMOD" => TA_CDLHIKKAKEMOD(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLHOMINGPIGEON" => TA_CDLHOMINGPIGEON(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLIDENTICAL3CROWS" => TA_CDLIDENTICAL3CROWS(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLINNECK" => TA_CDLINNECK(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLINVERTEDHAMMER" => TA_CDLINVERTEDHAMMER(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLKICKING" => TA_CDLKICKING(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLKICKINGBYLENGTH" => TA_CDLKICKINGBYLENGTH(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLLADDERBOTTOM" => TA_CDLLADDERBOTTOM(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLLONGLEGGEDDOJI" => TA_CDLLONGLEGGEDDOJI(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLLONGLINE" => TA_CDLLONGLINE(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLMARUBOZU" => TA_CDLMARUBOZU(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLMATCHINGLOW" => TA_CDLMATCHINGLOW(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLMATHOLD" => TA_CDLMATHOLD(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLMORNINGDOJISTAR" => TA_CDLMORNINGDOJISTAR(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLMORNINGSTAR" => TA_CDLMORNINGSTAR(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLONNECK" => TA_CDLONNECK(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLPIERCING" => TA_CDLPIERCING(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLRICKSHAWMAN" => TA_CDLRICKSHAWMAN(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLRISEFALL3METHODS" => TA_CDLRISEFALL3METHODS(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLSEPARATINGLINES" => TA_CDLSEPARATINGLINES(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLSHOOTINGSTAR" => TA_CDLSHOOTINGSTAR(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLSHORTLINE" => TA_CDLSHORTLINE(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLSPINNINGTOP" => TA_CDLSPINNINGTOP(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLSTALLEDPATTERN" => TA_CDLSTALLEDPATTERN(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLSTICKSANDWICH" => TA_CDLSTICKSANDWICH(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLTAKURI" => TA_CDLTAKURI(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLTASUKIGAP" => TA_CDLTASUKIGAP(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLTHRUSTING" => TA_CDLTHRUSTING(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLTRISTAR" => TA_CDLTRISTAR(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLUNIQUE3RIVER" => TA_CDLUNIQUE3RIVER(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLUPSIDEGAP2CROWS" => TA_CDLUPSIDEGAP2CROWS(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                "CDLXSIDEGAP3METHODS" => TA_CDLXSIDEGAP3METHODS(0, n as TA_Integer - 1, open.as_ptr(), high.as_ptr(), low.as_ptr(), close.as_ptr(), &mut beg, &mut nb, out.as_mut_ptr()),
                    _ => return None,
                };
                if rc != TA_SUCCESS { return None; }
                checksum += out[(nb - 1) as usize];
            }
            let elapsed = start.elapsed();
            let ns_per_elem = elapsed.as_nanos() as f64 / (iters as f64 * n as f64);
            Some((ns_per_elem, checksum))
        }
    }
}




use std::time::Instant;

const N: usize = 100_000;
const BUDGET_NS: u128 = 80_000_000;

#[derive(Debug)]
struct BenchRow {
    name: String,
    group: String,
    adaq_ns: f64,
    c_ns: f64,
    parity: f64,
    c_missing: bool,
}

#[cfg(feature = "bench-c")]
fn make_inputs() -> (Vec<f64>, Vec<f64>, Vec<f64>, Vec<f64>, Vec<f64>, Vec<f64>, Vec<f64>) {
    let mut x = 12345.0f64;
    let mut real0 = Vec::with_capacity(N);
    let mut real1 = Vec::with_capacity(N);
    let mut close = Vec::with_capacity(N);
    let mut open = Vec::with_capacity(N);
    let mut high = Vec::with_capacity(N);
    let mut low = Vec::with_capacity(N);
    let mut vol = Vec::with_capacity(N);
    for _ in 0..N {
        x = (x * 1103515245.0 + 12345.0) % 1e9;
        let base = 50.0 + (x / 1e9) * 10.0;
        let c = base;
        close.push(c);
        real0.push(c);
        high.push(c + 0.5);
        low.push(c - 0.5);
        open.push(c + 0.1);
        vol.push(1.0e6);
        let y = (x * 9301.0 + 49297.0) % 1e9;
        real1.push(40.0 + (y / 1e9) * 10.0);
    }
    (real0, real1, open, high, low, close, vol)
}

#[cfg(feature = "bench-c")]
fn ma_type_from_i32(v: i32) -> adaq_talib::overlap::MaType {
    use adaq_talib::overlap::MaType::*;
    match v {
        0 => Sma, 1 => Ema, 2 => Wma, 3 => Dema, 4 => Tema, 5 => Trima, 6 => Kama, 7 => Mama,
        _ => Sma,
    }
}

#[cfg(feature = "bench-c")]
fn run_all() -> Vec<BenchRow> {
    unsafe { talib_ffi::TA_Initialize(); }
    let (real0, real1, open, high, low, close, vol) = make_inputs();
    let mut rows: Vec<BenchRow> = Vec::new();

    // === sma (TA::SMA) ---
    {
        let ta_name = "SMA";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::overlap::sma_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::sma_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::sma_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "sma", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "sma".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === ema (TA::EMA) ---
    {
        let ta_name = "EMA";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::overlap::ema_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::ema_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::ema_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "ema", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "ema".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === wma (TA::WMA) ---
    {
        let ta_name = "WMA";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::overlap::wma_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::wma_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::wma_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "wma", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "wma".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === dema (TA::DEMA) ---
    {
        let ta_name = "DEMA";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::overlap::dema_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::dema_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::dema_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "dema", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "dema".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === tema (TA::TEMA) ---
    {
        let ta_name = "TEMA";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::overlap::tema_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::tema_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::tema_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "tema", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "tema".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === midpoint (TA::MIDPOINT) ---
    {
        let ta_name = "MIDPOINT";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::overlap::midpoint_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::midpoint_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::midpoint_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "midpoint", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "midpoint".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === midprice (TA::MIDPRICE) ---
    {
        let ta_name = "MIDPRICE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::overlap::midprice_with_output(&high, &low, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::midprice_with_output(&high, &low, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::midprice_with_output(&high, &low, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "midprice", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "midprice".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === bbands (TA::BBANDS) ---
    {
        let ta_name = "BBANDS";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut s = adaq_talib::overlap::Bbands { upper: vec![0.0; N], middle: vec![0.0; N], lower: vec![0.0; N] };
        adaq_talib::overlap::bbands_with_output(&real0, opt_vals[0].as_int() as usize, opt_vals[1].as_real(), opt_vals[2].as_real(), ma_type_from_i32(opt_vals[3].as_int()), &mut s).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::bbands_with_output(&real0, opt_vals[0].as_int() as usize, opt_vals[1].as_real(), opt_vals[2].as_real(), ma_type_from_i32(opt_vals[3].as_int()), &mut s).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::bbands_with_output(&real0, opt_vals[0].as_int() as usize, opt_vals[1].as_real(), opt_vals[2].as_real(), ma_type_from_i32(opt_vals[3].as_int()), &mut s).unwrap(); _ack += s.upper[N - 1] + s.middle[N - 1] + s.lower[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "bbands", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "bbands".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === accbands (TA::ACCBANDS) ---
    {
        let ta_name = "ACCBANDS";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut s = adaq_talib::overlap::AccBands { upper: vec![0.0; N], middle: vec![0.0; N], lower: vec![0.0; N] };
        adaq_talib::overlap::accbands_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut s).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::accbands_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut s).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::accbands_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut s).unwrap(); _ack += s.upper[N - 1] + s.middle[N - 1] + s.lower[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "accbands", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "accbands".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === trima (TA::TRIMA) ---
    {
        let ta_name = "TRIMA";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::overlap::trima_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::trima_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::trima_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "trima", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "trima".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === t3 (TA::T3) ---
    {
        let ta_name = "T3";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::overlap::t3_with_output(&real0, opt_vals[0].as_int() as usize, opt_vals[1].as_real(), &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::t3_with_output(&real0, opt_vals[0].as_int() as usize, opt_vals[1].as_real(), &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::t3_with_output(&real0, opt_vals[0].as_int() as usize, opt_vals[1].as_real(), &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "t3", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "t3".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === ma (TA::MA) ---
    {
        let ta_name = "MA";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::overlap::ma_with_output(&real0, opt_vals[0].as_int() as usize, ma_type_from_i32(opt_vals[1].as_int()), &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::ma_with_output(&real0, opt_vals[0].as_int() as usize, ma_type_from_i32(opt_vals[1].as_int()), &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::ma_with_output(&real0, opt_vals[0].as_int() as usize, ma_type_from_i32(opt_vals[1].as_int()), &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "ma", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "ma".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === mavp (TA::MAVP) ---
    {
        let ta_name = "MAVP";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::overlap::mavp_with_output(&real0, &real1, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize, ma_type_from_i32(opt_vals[2].as_int()), &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::mavp_with_output(&real0, &real1, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize, ma_type_from_i32(opt_vals[2].as_int()), &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::mavp_with_output(&real0, &real1, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize, ma_type_from_i32(opt_vals[2].as_int()), &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "mavp", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "mavp".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === kama (TA::KAMA) ---
    {
        let ta_name = "KAMA";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::overlap::kama_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::kama_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::kama_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "kama", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "kama".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === sar (TA::SAR) ---
    {
        let ta_name = "SAR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::overlap::sar_with_output(&high, &low, opt_vals[0].as_real(), opt_vals[1].as_real(), &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::sar_with_output(&high, &low, opt_vals[0].as_real(), opt_vals[1].as_real(), &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::sar_with_output(&high, &low, opt_vals[0].as_real(), opt_vals[1].as_real(), &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "sar", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "sar".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === sarext (TA::SAREXT) ---
    {
        let ta_name = "SAREXT";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::overlap::sarext_with_output(&high, &low, opt_vals[0].as_real(), opt_vals[1].as_real(), opt_vals[2].as_real(), opt_vals[3].as_real(), opt_vals[4].as_real(), opt_vals[5].as_real(), opt_vals[6].as_real(), opt_vals[7].as_real(), &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::overlap::sarext_with_output(&high, &low, opt_vals[0].as_real(), opt_vals[1].as_real(), opt_vals[2].as_real(), opt_vals[3].as_real(), opt_vals[4].as_real(), opt_vals[5].as_real(), opt_vals[6].as_real(), opt_vals[7].as_real(), &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::overlap::sarext_with_output(&high, &low, opt_vals[0].as_real(), opt_vals[1].as_real(), opt_vals[2].as_real(), opt_vals[3].as_real(), opt_vals[4].as_real(), opt_vals[5].as_real(), opt_vals[6].as_real(), opt_vals[7].as_real(), &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "sarext", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "sarext".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === mom (TA::MOM) ---
    {
        let ta_name = "MOM";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::mom_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::mom_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::mom_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "mom", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "mom".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === roc (TA::ROC) ---
    {
        let ta_name = "ROC";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::roc_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::roc_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::roc_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "roc", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "roc".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === rocp (TA::ROCP) ---
    {
        let ta_name = "ROCP";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::rocp_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::rocp_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::rocp_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "rocp", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "rocp".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === rocr (TA::ROCR) ---
    {
        let ta_name = "ROCR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::rocr_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::rocr_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::rocr_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "rocr", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "rocr".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === rocr100 (TA::ROCR100) ---
    {
        let ta_name = "ROCR100";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::rocr100_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::rocr100_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::rocr100_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "rocr100", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "rocr100".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === rsi (TA::RSI) ---
    {
        let ta_name = "RSI";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::rsi_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::rsi_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::rsi_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "rsi", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "rsi".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === macd (TA::MACD) ---
    {
        let ta_name = "MACD";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut s = adaq_talib::momentum::Macd { macd: vec![0.0; N], signal: vec![0.0; N], hist: vec![0.0; N] };
        adaq_talib::momentum::macd_with_output(&real0, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize, opt_vals[2].as_int() as usize, &mut s).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::macd_with_output(&real0, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize, opt_vals[2].as_int() as usize, &mut s).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::macd_with_output(&real0, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize, opt_vals[2].as_int() as usize, &mut s).unwrap(); _ack += s.macd[N - 1] + s.signal[N - 1] + s.hist[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "macd", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "macd".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === macd_fix (TA::MACDFIX) ---
    {
        let ta_name = "MACDFIX";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut s = adaq_talib::momentum::Macd { macd: vec![0.0; N], signal: vec![0.0; N], hist: vec![0.0; N] };
        adaq_talib::momentum::macd_fix_with_output(&real0, 12 as usize, 26 as usize, &mut s).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::macd_fix_with_output(&real0, 12 as usize, 26 as usize, &mut s).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::macd_fix_with_output(&real0, 12 as usize, 26 as usize, &mut s).unwrap(); _ack += s.macd[N - 1] + s.signal[N - 1] + s.hist[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match {{                 let _copts: Vec<talib_ffi::OptVal> = vec![talib_ffi::OptVal::Int(12), talib_ffi::OptVal::Int(26), talib_ffi::OptVal::Int(9)];                 talib_ffi::c_abstract("MACD", N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &_copts)             }} {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "macd_fix", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "macd_fix".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === macd_ext (TA::MACDEXT) ---
    {
        let ta_name = "MACDEXT";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut s = adaq_talib::momentum::Macd { macd: vec![0.0; N], signal: vec![0.0; N], hist: vec![0.0; N] };
        adaq_talib::momentum::macd_ext_with_output(&real0, 12 as usize, 26 as usize, 9 as usize, &mut s).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::macd_ext_with_output(&real0, 12 as usize, 26 as usize, 9 as usize, &mut s).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::macd_ext_with_output(&real0, 12 as usize, 26 as usize, 9 as usize, &mut s).unwrap(); _ack += s.macd[N - 1] + s.signal[N - 1] + s.hist[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match {{                 let _copts: Vec<talib_ffi::OptVal> = vec![talib_ffi::OptVal::Int(12), talib_ffi::OptVal::Int(1), talib_ffi::OptVal::Int(26), talib_ffi::OptVal::Int(1), talib_ffi::OptVal::Int(9), talib_ffi::OptVal::Int(1)];                 talib_ffi::c_abstract("MACDEXT", N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &_copts)             }} {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "macd_ext", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "macd_ext".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === apo (TA::APO) ---
    {
        let ta_name = "APO";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::apo_with_output(&real0, 12 as usize, 26 as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::apo_with_output(&real0, 12 as usize, 26 as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::apo_with_output(&real0, 12 as usize, 26 as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match {{                 let _copts: Vec<talib_ffi::OptVal> = vec![talib_ffi::OptVal::Int(12), talib_ffi::OptVal::Int(26), talib_ffi::OptVal::Int(1)];                 talib_ffi::c_abstract("APO", N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &_copts)             }} {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "apo", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "apo".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === ppo (TA::PPO) ---
    {
        let ta_name = "PPO";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::ppo_with_output(&real0, 12 as usize, 26 as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::ppo_with_output(&real0, 12 as usize, 26 as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::ppo_with_output(&real0, 12 as usize, 26 as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match {{                 let _copts: Vec<talib_ffi::OptVal> = vec![talib_ffi::OptVal::Int(12), talib_ffi::OptVal::Int(26), talib_ffi::OptVal::Int(1)];                 talib_ffi::c_abstract("PPO", N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &_copts)             }} {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "ppo", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "ppo".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cmo (TA::CMO) ---
    {
        let ta_name = "CMO";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::cmo_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::cmo_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::cmo_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cmo", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cmo".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cci (TA::CCI) ---
    {
        let ta_name = "CCI";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::cci_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::cci_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::cci_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cci", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cci".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === mfi (TA::MFI) ---
    {
        let ta_name = "MFI";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::mfi_with_output(&high, &low, &close, &vol, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::mfi_with_output(&high, &low, &close, &vol, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::mfi_with_output(&high, &low, &close, &vol, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "mfi", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "mfi".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === willr (TA::WILLR) ---
    {
        let ta_name = "WILLR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::willr_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::willr_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::willr_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "willr", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "willr".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === bop (TA::BOP) ---
    {
        let ta_name = "BOP";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::bop_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::bop_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::bop_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "bop", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "bop".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === ultosc (TA::ULTOSC) ---
    {
        let ta_name = "ULTOSC";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::ultosc_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize, opt_vals[2].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::ultosc_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize, opt_vals[2].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::ultosc_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize, opt_vals[2].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "ultosc", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "ultosc".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === plus_dm (TA::PLUS_DM) ---
    {
        let ta_name = "PLUS_DM";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::plus_dm_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::plus_dm_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::plus_dm_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "plus_dm", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "plus_dm".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === minus_dm (TA::MINUS_DM) ---
    {
        let ta_name = "MINUS_DM";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::minus_dm_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::minus_dm_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::minus_dm_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "minus_dm", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "minus_dm".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === plus_di (TA::PLUS_DI) ---
    {
        let ta_name = "PLUS_DI";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::plus_di_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::plus_di_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::plus_di_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "plus_di", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "plus_di".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === minus_di (TA::MINUS_DI) ---
    {
        let ta_name = "MINUS_DI";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::minus_di_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::minus_di_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::minus_di_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "minus_di", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "minus_di".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === adx (TA::ADX) ---
    {
        let ta_name = "ADX";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::adx_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::adx_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::adx_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "adx", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "adx".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === adxr (TA::ADXR) ---
    {
        let ta_name = "ADXR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::adxr_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::adxr_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::adxr_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "adxr", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "adxr".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === aroon (TA::AROON) ---
    {
        let ta_name = "AROON";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut s = adaq_talib::momentum::Aroon { up: vec![0.0; N], down: vec![0.0; N] };
        adaq_talib::momentum::aroon_with_output(&high, &low, opt_vals[0].as_int() as usize, &mut s).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::aroon_with_output(&high, &low, opt_vals[0].as_int() as usize, &mut s).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::aroon_with_output(&high, &low, opt_vals[0].as_int() as usize, &mut s).unwrap(); _ack += s.up[N - 1] + s.down[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "aroon", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "aroon".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === aroon_osc (TA::AROONOSC) ---
    {
        let ta_name = "AROONOSC";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::momentum::aroon_osc_with_output(&high, &low, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::aroon_osc_with_output(&high, &low, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::aroon_osc_with_output(&high, &low, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "aroon_osc", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "aroon_osc".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === stoch (TA::STOCH) ---
    {
        let ta_name = "STOCH";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut s = adaq_talib::momentum::Stoch { slow_k: vec![0.0; N], slow_d: vec![0.0; N] };
        adaq_talib::momentum::stoch_with_output(&high, &low, &close, 5 as usize, 3 as usize, 3 as usize, &mut s).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::stoch_with_output(&high, &low, &close, 5 as usize, 3 as usize, 3 as usize, &mut s).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::stoch_with_output(&high, &low, &close, 5 as usize, 3 as usize, 3 as usize, &mut s).unwrap(); _ack += s.slow_k[N - 1] + s.slow_d[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "stoch", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "stoch".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === stoch_f (TA::STOCHF) ---
    {
        let ta_name = "STOCHF";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut s = adaq_talib::momentum::StochF { fast_k: vec![0.0; N], fast_d: vec![0.0; N] };
        adaq_talib::momentum::stoch_f_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize, &mut s).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::momentum::stoch_f_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize, &mut s).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::momentum::stoch_f_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize, &mut s).unwrap(); _ack += s.fast_k[N - 1] + s.fast_d[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "stoch_f", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "stoch_f".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === stoch_rsi (TA::STOCHRSI) ---
    {
        let ta_name = "STOCHRSI";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        
        let _o = adaq_talib::momentum::stoch_rsi(&close, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { let _o = adaq_talib::momentum::stoch_rsi(&close, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { let _o = adaq_talib::momentum::stoch_rsi(&close, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize).unwrap(); _ack += _o[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "stoch_rsi", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "stoch_rsi".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === trix (TA::TRIX) ---
    {
        let ta_name = "TRIX";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        
        let _o = adaq_talib::momentum::trix(&real0, opt_vals[0].as_int() as usize).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { let _o = adaq_talib::momentum::trix(&real0, opt_vals[0].as_int() as usize).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { let _o = adaq_talib::momentum::trix(&real0, opt_vals[0].as_int() as usize).unwrap(); _ack += _o[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "trix", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "trix".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === dx (TA::DX) ---
    {
        let ta_name = "DX";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        
        let _o = adaq_talib::momentum::dx(&high, &low, &close, opt_vals[0].as_int() as usize).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { let _o = adaq_talib::momentum::dx(&high, &low, &close, opt_vals[0].as_int() as usize).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { let _o = adaq_talib::momentum::dx(&high, &low, &close, opt_vals[0].as_int() as usize).unwrap(); _ack += _o[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "dx", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "dx".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === imi (TA::IMI) ---
    {
        let ta_name = "IMI";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        
        let _o = adaq_talib::momentum::imi(&open, &close, opt_vals[0].as_int() as usize).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { let _o = adaq_talib::momentum::imi(&open, &close, opt_vals[0].as_int() as usize).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { let _o = adaq_talib::momentum::imi(&open, &close, opt_vals[0].as_int() as usize).unwrap(); _ack += _o[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "imi", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "imi".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === trange (TA::TRANGE) ---
    {
        let ta_name = "TRANGE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::volatility::trange_with_output(&high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::volatility::trange_with_output(&high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::volatility::trange_with_output(&high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "trange", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "trange".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === atr (TA::ATR) ---
    {
        let ta_name = "ATR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::volatility::atr_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::volatility::atr_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::volatility::atr_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "atr", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "atr".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === natr (TA::NATR) ---
    {
        let ta_name = "NATR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::volatility::natr_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::volatility::natr_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::volatility::natr_with_output(&high, &low, &close, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "natr", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "natr".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === ad (TA::AD) ---
    {
        let ta_name = "AD";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::volume::ad_with_output(&high, &low, &close, &vol, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::volume::ad_with_output(&high, &low, &close, &vol, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::volume::ad_with_output(&high, &low, &close, &vol, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "ad", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "ad".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === adosc (TA::ADOSC) ---
    {
        let ta_name = "ADOSC";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::volume::adosc_with_output(&high, &low, &close, &vol, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::volume::adosc_with_output(&high, &low, &close, &vol, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::volume::adosc_with_output(&high, &low, &close, &vol, opt_vals[0].as_int() as usize, opt_vals[1].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "adosc", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "adosc".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === obv (TA::OBV) ---
    {
        let ta_name = "OBV";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::volume::obv_with_output(&close, &vol, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::volume::obv_with_output(&close, &vol, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::volume::obv_with_output(&close, &vol, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "obv", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "obv".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === avgprice (TA::AVGPRICE) ---
    {
        let ta_name = "AVGPRICE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::price_transform::avgprice_with_output(&high, &low, &close, &open, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::price_transform::avgprice_with_output(&high, &low, &close, &open, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::price_transform::avgprice_with_output(&high, &low, &close, &open, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "avgprice", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "avgprice".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === medprice (TA::MEDPRICE) ---
    {
        let ta_name = "MEDPRICE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::price_transform::medprice_with_output(&high, &low, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::price_transform::medprice_with_output(&high, &low, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::price_transform::medprice_with_output(&high, &low, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "medprice", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "medprice".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === typprice (TA::TYPPRICE) ---
    {
        let ta_name = "TYPPRICE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::price_transform::typprice_with_output(&high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::price_transform::typprice_with_output(&high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::price_transform::typprice_with_output(&high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "typprice", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "typprice".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === wclprice (TA::WCLPRICE) ---
    {
        let ta_name = "WCLPRICE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::price_transform::wclprice_with_output(&high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::price_transform::wclprice_with_output(&high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::price_transform::wclprice_with_output(&high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "wclprice", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "wclprice".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === avgdev (TA::AVGDEV) ---
    {
        let ta_name = "AVGDEV";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::price_transform::avgdev_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::price_transform::avgdev_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::price_transform::avgdev_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "avgdev", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "avgdev".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === stddev (TA::STDDEV) ---
    {
        let ta_name = "STDDEV";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::stat::stddev_with_output(&real0, opt_vals[0].as_int() as usize, opt_vals[1].as_real(), &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::stat::stddev_with_output(&real0, opt_vals[0].as_int() as usize, opt_vals[1].as_real(), &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::stat::stddev_with_output(&real0, opt_vals[0].as_int() as usize, opt_vals[1].as_real(), &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "stddev", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "stddev".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === var (TA::VAR) ---
    {
        let ta_name = "VAR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::stat::var_with_output(&real0, opt_vals[0].as_int() as usize, opt_vals[1].as_real(), &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::stat::var_with_output(&real0, opt_vals[0].as_int() as usize, opt_vals[1].as_real(), &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::stat::var_with_output(&real0, opt_vals[0].as_int() as usize, opt_vals[1].as_real(), &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "var", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "var".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === linear_reg (TA::LINEARREG) ---
    {
        let ta_name = "LINEARREG";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::stat::linear_reg_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::stat::linear_reg_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::stat::linear_reg_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "linear_reg", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "linear_reg".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === linear_reg_angle (TA::LINEARREG_ANGLE) ---
    {
        let ta_name = "LINEARREG_ANGLE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::stat::linear_reg_angle_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::stat::linear_reg_angle_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::stat::linear_reg_angle_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "linear_reg_angle", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "linear_reg_angle".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === linear_reg_intercept (TA::LINEARREG_INTERCEPT) ---
    {
        let ta_name = "LINEARREG_INTERCEPT";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::stat::linear_reg_intercept_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::stat::linear_reg_intercept_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::stat::linear_reg_intercept_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "linear_reg_intercept", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "linear_reg_intercept".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === linear_reg_slope (TA::LINEARREG_SLOPE) ---
    {
        let ta_name = "LINEARREG_SLOPE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::stat::linear_reg_slope_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::stat::linear_reg_slope_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::stat::linear_reg_slope_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "linear_reg_slope", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "linear_reg_slope".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === tsf (TA::TSF) ---
    {
        let ta_name = "TSF";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::stat::tsf_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::stat::tsf_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::stat::tsf_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "tsf", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "tsf".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === beta (TA::BETA) ---
    {
        let ta_name = "BETA";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::stat::beta_with_output(&real0, &real1, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::stat::beta_with_output(&real0, &real1, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::stat::beta_with_output(&real0, &real1, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "beta", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "beta".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === correl (TA::CORREL) ---
    {
        let ta_name = "CORREL";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::stat::correl_with_output(&real0, &real1, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::stat::correl_with_output(&real0, &real1, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::stat::correl_with_output(&real0, &real1, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "correl", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "correl".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === add (TA::ADD) ---
    {
        let ta_name = "ADD";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_ops::add_with_output(&real0, &real1, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_ops::add_with_output(&real0, &real1, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_ops::add_with_output(&real0, &real1, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "add", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "add".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === sub (TA::SUB) ---
    {
        let ta_name = "SUB";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_ops::sub_with_output(&real0, &real1, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_ops::sub_with_output(&real0, &real1, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_ops::sub_with_output(&real0, &real1, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "sub", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "sub".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === mult (TA::MULT) ---
    {
        let ta_name = "MULT";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_ops::mult_with_output(&real0, &real1, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_ops::mult_with_output(&real0, &real1, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_ops::mult_with_output(&real0, &real1, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "mult", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "mult".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === div (TA::DIV) ---
    {
        let ta_name = "DIV";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_ops::div_with_output(&real0, &real1, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_ops::div_with_output(&real0, &real1, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_ops::div_with_output(&real0, &real1, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "div", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "div".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === max (TA::MAX) ---
    {
        let ta_name = "MAX";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_ops::max_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_ops::max_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_ops::max_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "max", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "max".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === min (TA::MIN) ---
    {
        let ta_name = "MIN";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_ops::min_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_ops::min_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_ops::min_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "min", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "min".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === sum (TA::SUM) ---
    {
        let ta_name = "SUM";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_ops::sum_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_ops::sum_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_ops::sum_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "sum", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "sum".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === max_index (TA::MAXINDEX) ---
    {
        let ta_name = "MAXINDEX";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_ops::max_index_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_ops::max_index_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_ops::max_index_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "max_index", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "max_index".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === min_index (TA::MININDEX) ---
    {
        let ta_name = "MININDEX";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_ops::min_index_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_ops::min_index_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_ops::min_index_with_output(&real0, opt_vals[0].as_int() as usize, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "min_index", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "min_index".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === minmax (TA::MINMAX) ---
    {
        let ta_name = "MINMAX";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut s = adaq_talib::math_ops::MinMax { min: vec![0.0; N], max: vec![0.0; N] };
        adaq_talib::math_ops::minmax_with_output(&real0, opt_vals[0].as_int() as usize, &mut s).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_ops::minmax_with_output(&real0, opt_vals[0].as_int() as usize, &mut s).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_ops::minmax_with_output(&real0, opt_vals[0].as_int() as usize, &mut s).unwrap(); _ack += s.min[N - 1] + s.max[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "minmax", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "minmax".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === minmax_index (TA::MINMAXINDEX) ---
    {
        let ta_name = "MINMAXINDEX";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut s = adaq_talib::math_ops::MinMaxIndex { min_idx: vec![0.0; N], max_idx: vec![0.0; N] };
        adaq_talib::math_ops::minmax_index_with_output(&real0, opt_vals[0].as_int() as usize, &mut s).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_ops::minmax_index_with_output(&real0, opt_vals[0].as_int() as usize, &mut s).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_ops::minmax_index_with_output(&real0, opt_vals[0].as_int() as usize, &mut s).unwrap(); _ack += s.min_idx[N - 1] + s.max_idx[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "minmax_index", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "minmax_index".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === acos (TA::ACOS) ---
    {
        let ta_name = "ACOS";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_trans::acos_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_trans::acos_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_trans::acos_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "acos", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "acos".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === asin (TA::ASIN) ---
    {
        let ta_name = "ASIN";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_trans::asin_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_trans::asin_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_trans::asin_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "asin", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "asin".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === atan (TA::ATAN) ---
    {
        let ta_name = "ATAN";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_trans::atan_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_trans::atan_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_trans::atan_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "atan", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "atan".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === ceil (TA::CEIL) ---
    {
        let ta_name = "CEIL";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_trans::ceil_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_trans::ceil_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_trans::ceil_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "ceil", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "ceil".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cos (TA::COS) ---
    {
        let ta_name = "COS";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_trans::cos_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_trans::cos_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_trans::cos_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cos", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cos".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cosh (TA::COSH) ---
    {
        let ta_name = "COSH";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_trans::cosh_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_trans::cosh_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_trans::cosh_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cosh", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cosh".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === exp (TA::EXP) ---
    {
        let ta_name = "EXP";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_trans::exp_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_trans::exp_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_trans::exp_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "exp", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "exp".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === floor (TA::FLOOR) ---
    {
        let ta_name = "FLOOR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_trans::floor_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_trans::floor_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_trans::floor_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "floor", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "floor".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === ln (TA::LN) ---
    {
        let ta_name = "LN";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_trans::ln_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_trans::ln_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_trans::ln_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "ln", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "ln".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === log10 (TA::LOG10) ---
    {
        let ta_name = "LOG10";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_trans::log10_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_trans::log10_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_trans::log10_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "log10", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "log10".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === sin (TA::SIN) ---
    {
        let ta_name = "SIN";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_trans::sin_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_trans::sin_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_trans::sin_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "sin", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "sin".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === sinh (TA::SINH) ---
    {
        let ta_name = "SINH";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_trans::sinh_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_trans::sinh_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_trans::sinh_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "sinh", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "sinh".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === sqrt (TA::SQRT) ---
    {
        let ta_name = "SQRT";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_trans::sqrt_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_trans::sqrt_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_trans::sqrt_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "sqrt", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "sqrt".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === tan (TA::TAN) ---
    {
        let ta_name = "TAN";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_trans::tan_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_trans::tan_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_trans::tan_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "tan", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "tan".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === tanh (TA::TANH) ---
    {
        let ta_name = "TANH";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::math_trans::tanh_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::math_trans::tanh_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::math_trans::tanh_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "tanh", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "tanh".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === mama (TA::MAMA) ---
    {
        let ta_name = "MAMA";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        
        let _s = adaq_talib::cycle::mama(&real0, opt_vals[0].as_real(), opt_vals[1].as_real()).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { let _s = adaq_talib::cycle::mama(&real0, opt_vals[0].as_real(), opt_vals[1].as_real()).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { let _s = adaq_talib::cycle::mama(&real0, opt_vals[0].as_real(), opt_vals[1].as_real()).unwrap(); _ack += _s.mama[N - 1] + _s.fama[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "mama", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "mama".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === ht_trendline (TA::HT_TRENDLINE) ---
    {
        let ta_name = "HT_TRENDLINE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::cycle::ht_trendline_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::cycle::ht_trendline_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::cycle::ht_trendline_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "ht_trendline", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "ht_trendline".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === ht_dcperiod (TA::HT_DCPERIOD) ---
    {
        let ta_name = "HT_DCPERIOD";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::cycle::ht_dcperiod_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::cycle::ht_dcperiod_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::cycle::ht_dcperiod_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "ht_dcperiod", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "ht_dcperiod".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === ht_dcphase (TA::HT_DCPHASE) ---
    {
        let ta_name = "HT_DCPHASE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::cycle::ht_dcphase_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::cycle::ht_dcphase_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::cycle::ht_dcphase_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "ht_dcphase", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "ht_dcphase".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === ht_phasor (TA::HT_PHASOR) ---
    {
        let ta_name = "HT_PHASOR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut s = adaq_talib::cycle::HtPhasor { in_phase: vec![0.0; N], quadrature: vec![0.0; N] };
        adaq_talib::cycle::ht_phasor_with_output(&real0, &mut s).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::cycle::ht_phasor_with_output(&real0, &mut s).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::cycle::ht_phasor_with_output(&real0, &mut s).unwrap(); _ack += s.in_phase[N - 1] + s.quadrature[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "ht_phasor", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "ht_phasor".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === ht_sine (TA::HT_SINE) ---
    {
        let ta_name = "HT_SINE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut s = adaq_talib::cycle::HtSine { sine: vec![0.0; N], lead_sine: vec![0.0; N] };
        adaq_talib::cycle::ht_sine_with_output(&real0, &mut s).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::cycle::ht_sine_with_output(&real0, &mut s).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::cycle::ht_sine_with_output(&real0, &mut s).unwrap(); _ack += s.sine[N - 1] + s.lead_sine[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "ht_sine", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "ht_sine".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === ht_trendmode (TA::HT_TRENDMODE) ---
    {
        let ta_name = "HT_TRENDMODE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::cycle::ht_trendmode_with_output(&real0, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::cycle::ht_trendmode_with_output(&real0, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::cycle::ht_trendmode_with_output(&real0, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "ht_trendmode", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "ht_trendmode".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_doji (TA::CDLDOJI) ---
    {
        let ta_name = "CDLDOJI";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_doji_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_doji_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_doji_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_doji", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_doji".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_marubozu (TA::CDLMARUBOZU) ---
    {
        let ta_name = "CDLMARUBOZU";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_marubozu_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_marubozu_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_marubozu_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_marubozu", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_marubozu".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_hammer (TA::CDLHAMMER) ---
    {
        let ta_name = "CDLHAMMER";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_hammer_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_hammer_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_hammer_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_hammer", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_hammer".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_shootingstar (TA::CDLSHOOTINGSTAR) ---
    {
        let ta_name = "CDLSHOOTINGSTAR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_shootingstar_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_shootingstar_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_shootingstar_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_shootingstar", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_shootingstar".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_engulfing (TA::CDLENGULFING) ---
    {
        let ta_name = "CDLENGULFING";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_engulfing_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_engulfing_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_engulfing_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_engulfing", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_engulfing".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_harami (TA::CDLHARAMI) ---
    {
        let ta_name = "CDLHARAMI";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_harami_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_harami_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_harami_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_harami", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_harami".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_highwave (TA::CDLHIGHWAVE) ---
    {
        let ta_name = "CDLHIGHWAVE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_highwave_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_highwave_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_highwave_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_highwave", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_highwave".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_2crows (TA::CDL2CROWS) ---
    {
        let ta_name = "CDL2CROWS";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_2crows_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_2crows_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_2crows_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_2crows", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_2crows".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_3blackcrows (TA::CDL3BLACKCROWS) ---
    {
        let ta_name = "CDL3BLACKCROWS";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_3blackcrows_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_3blackcrows_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_3blackcrows_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_3blackcrows", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_3blackcrows".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_3inside (TA::CDL3INSIDE) ---
    {
        let ta_name = "CDL3INSIDE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_3inside_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_3inside_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_3inside_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_3inside", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_3inside".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_3linestrike (TA::CDL3LINESTRIKE) ---
    {
        let ta_name = "CDL3LINESTRIKE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_3linestrike_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_3linestrike_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_3linestrike_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_3linestrike", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_3linestrike".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_3outside (TA::CDL3OUTSIDE) ---
    {
        let ta_name = "CDL3OUTSIDE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_3outside_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_3outside_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_3outside_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_3outside", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_3outside".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_3starsinsouth (TA::CDL3STARSINSOUTH) ---
    {
        let ta_name = "CDL3STARSINSOUTH";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_3starsinsouth_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_3starsinsouth_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_3starsinsouth_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_3starsinsouth", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_3starsinsouth".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_3whitesoldiers (TA::CDL3WHITESOLDIERS) ---
    {
        let ta_name = "CDL3WHITESOLDIERS";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_3whitesoldiers_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_3whitesoldiers_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_3whitesoldiers_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_3whitesoldiers", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_3whitesoldiers".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_abandonedbaby (TA::CDLABANDONEDBABY) ---
    {
        let ta_name = "CDLABANDONEDBABY";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_abandonedbaby_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_abandonedbaby_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_abandonedbaby_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_abandonedbaby", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_abandonedbaby".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_advanceblock (TA::CDLADVANCEBLOCK) ---
    {
        let ta_name = "CDLADVANCEBLOCK";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_advanceblock_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_advanceblock_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_advanceblock_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_advanceblock", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_advanceblock".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_belthold (TA::CDLBELTHOLD) ---
    {
        let ta_name = "CDLBELTHOLD";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_belthold_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_belthold_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_belthold_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_belthold", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_belthold".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_breakaway (TA::CDLBREAKAWAY) ---
    {
        let ta_name = "CDLBREAKAWAY";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_breakaway_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_breakaway_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_breakaway_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_breakaway", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_breakaway".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_closingmarubozu (TA::CDLCLOSINGMARUBOZU) ---
    {
        let ta_name = "CDLCLOSINGMARUBOZU";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_closingmarubozu_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_closingmarubozu_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_closingmarubozu_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_closingmarubozu", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_closingmarubozu".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_concealbabyswall (TA::CDLCONCEALBABYSWALL) ---
    {
        let ta_name = "CDLCONCEALBABYSWALL";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_concealbabyswall_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_concealbabyswall_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_concealbabyswall_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_concealbabyswall", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_concealbabyswall".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_counterattack (TA::CDLCOUNTERATTACK) ---
    {
        let ta_name = "CDLCOUNTERATTACK";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_counterattack_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_counterattack_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_counterattack_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_counterattack", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_counterattack".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_darkcloudcover (TA::CDLDARKCLOUDCOVER) ---
    {
        let ta_name = "CDLDARKCLOUDCOVER";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_darkcloudcover_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_darkcloudcover_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_darkcloudcover_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_darkcloudcover", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_darkcloudcover".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_dojistar (TA::CDLDOJISTAR) ---
    {
        let ta_name = "CDLDOJISTAR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_dojistar_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_dojistar_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_dojistar_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_dojistar", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_dojistar".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_dragonflydoji (TA::CDLDRAGONFLYDOJI) ---
    {
        let ta_name = "CDLDRAGONFLYDOJI";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_dragonflydoji_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_dragonflydoji_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_dragonflydoji_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_dragonflydoji", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_dragonflydoji".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_eveningdojistar (TA::CDLEVENINGDOJISTAR) ---
    {
        let ta_name = "CDLEVENINGDOJISTAR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_eveningdojistar_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_eveningdojistar_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_eveningdojistar_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_eveningdojistar", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_eveningdojistar".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_eveningstar (TA::CDLEVENINGSTAR) ---
    {
        let ta_name = "CDLEVENINGSTAR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_eveningstar_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_eveningstar_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_eveningstar_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_eveningstar", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_eveningstar".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_gapsidesidewhite (TA::CDLGAPSIDESIDEWHITE) ---
    {
        let ta_name = "CDLGAPSIDESIDEWHITE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_gapsidesidewhite_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_gapsidesidewhite_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_gapsidesidewhite_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_gapsidesidewhite", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_gapsidesidewhite".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_gravestonedoji (TA::CDLGRAVESTONEDOJI) ---
    {
        let ta_name = "CDLGRAVESTONEDOJI";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_gravestonedoji_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_gravestonedoji_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_gravestonedoji_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_gravestonedoji", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_gravestonedoji".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_hangingman (TA::CDLHANGINGMAN) ---
    {
        let ta_name = "CDLHANGINGMAN";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_hangingman_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_hangingman_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_hangingman_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_hangingman", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_hangingman".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_haramicross (TA::CDLHARAMICROSS) ---
    {
        let ta_name = "CDLHARAMICROSS";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_haramicross_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_haramicross_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_haramicross_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_haramicross", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_haramicross".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_hikkake (TA::CDLHIKKAKE) ---
    {
        let ta_name = "CDLHIKKAKE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_hikkake_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_hikkake_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_hikkake_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_hikkake", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_hikkake".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_hikkakemod (TA::CDLHIKKAKEMOD) ---
    {
        let ta_name = "CDLHIKKAKEMOD";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_hikkakemod_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_hikkakemod_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_hikkakemod_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_hikkakemod", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_hikkakemod".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_homingpigeon (TA::CDLHOMINGPIGEON) ---
    {
        let ta_name = "CDLHOMINGPIGEON";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_homingpigeon_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_homingpigeon_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_homingpigeon_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_homingpigeon", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_homingpigeon".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_identical3crows (TA::CDLIDENTICAL3CROWS) ---
    {
        let ta_name = "CDLIDENTICAL3CROWS";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_identical3crows_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_identical3crows_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_identical3crows_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_identical3crows", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_identical3crows".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_inneck (TA::CDLINNECK) ---
    {
        let ta_name = "CDLINNECK";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_inneck_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_inneck_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_inneck_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_inneck", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_inneck".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_invertedhammer (TA::CDLINVERTEDHAMMER) ---
    {
        let ta_name = "CDLINVERTEDHAMMER";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_invertedhammer_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_invertedhammer_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_invertedhammer_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_invertedhammer", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_invertedhammer".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_kicking (TA::CDLKICKING) ---
    {
        let ta_name = "CDLKICKING";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_kicking_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_kicking_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_kicking_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_kicking", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_kicking".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_kickingbylength (TA::CDLKICKINGBYLENGTH) ---
    {
        let ta_name = "CDLKICKINGBYLENGTH";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_kickingbylength_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_kickingbylength_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_kickingbylength_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_kickingbylength", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_kickingbylength".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_ladderbottom (TA::CDLLADDERBOTTOM) ---
    {
        let ta_name = "CDLLADDERBOTTOM";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_ladderbottom_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_ladderbottom_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_ladderbottom_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_ladderbottom", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_ladderbottom".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_longleggeddoji (TA::CDLLONGLEGGEDDOJI) ---
    {
        let ta_name = "CDLLONGLEGGEDDOJI";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_longleggeddoji_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_longleggeddoji_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_longleggeddoji_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_longleggeddoji", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_longleggeddoji".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_longline (TA::CDLLONGLINE) ---
    {
        let ta_name = "CDLLONGLINE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_longline_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_longline_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_longline_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_longline", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_longline".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_matchinglow (TA::CDLMATCHINGLOW) ---
    {
        let ta_name = "CDLMATCHINGLOW";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_matchinglow_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_matchinglow_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_matchinglow_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_matchinglow", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_matchinglow".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_mathold (TA::CDLMATHOLD) ---
    {
        let ta_name = "CDLMATHOLD";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_mathold_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_mathold_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_mathold_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_mathold", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_mathold".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_morningstar (TA::CDLMORNINGSTAR) ---
    {
        let ta_name = "CDLMORNINGSTAR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_morningstar_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_morningstar_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_morningstar_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_morningstar", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_morningstar".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_morningdojistar (TA::CDLMORNINGDOJISTAR) ---
    {
        let ta_name = "CDLMORNINGDOJISTAR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_morningdojistar_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_morningdojistar_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_morningdojistar_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_morningdojistar", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_morningdojistar".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_onneck (TA::CDLONNECK) ---
    {
        let ta_name = "CDLONNECK";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_onneck_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_onneck_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_onneck_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_onneck", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_onneck".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_piercing (TA::CDLPIERCING) ---
    {
        let ta_name = "CDLPIERCING";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_piercing_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_piercing_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_piercing_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_piercing", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_piercing".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_rickshawman (TA::CDLRICKSHAWMAN) ---
    {
        let ta_name = "CDLRICKSHAWMAN";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_rickshawman_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_rickshawman_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_rickshawman_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_rickshawman", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_rickshawman".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_risefall3methods (TA::CDLRISEFALL3METHODS) ---
    {
        let ta_name = "CDLRISEFALL3METHODS";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_risefall3methods_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_risefall3methods_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_risefall3methods_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_risefall3methods", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_risefall3methods".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_separatinglines (TA::CDLSEPARATINGLINES) ---
    {
        let ta_name = "CDLSEPARATINGLINES";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_separatinglines_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_separatinglines_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_separatinglines_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_separatinglines", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_separatinglines".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_shortline (TA::CDLSHORTLINE) ---
    {
        let ta_name = "CDLSHORTLINE";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_shortline_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_shortline_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_shortline_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_shortline", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_shortline".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_spinningtop (TA::CDLSPINNINGTOP) ---
    {
        let ta_name = "CDLSPINNINGTOP";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_spinningtop_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_spinningtop_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_spinningtop_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_spinningtop", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_spinningtop".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_stalledpattern (TA::CDLSTALLEDPATTERN) ---
    {
        let ta_name = "CDLSTALLEDPATTERN";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_stalledpattern_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_stalledpattern_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_stalledpattern_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_stalledpattern", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_stalledpattern".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_sticksandwich (TA::CDLSTICKSANDWICH) ---
    {
        let ta_name = "CDLSTICKSANDWICH";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_sticksandwich_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_sticksandwich_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_sticksandwich_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_sticksandwich", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_sticksandwich".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_takuri (TA::CDLTAKURI) ---
    {
        let ta_name = "CDLTAKURI";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_takuri_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_takuri_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_takuri_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_takuri", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_takuri".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_tasukigap (TA::CDLTASUKIGAP) ---
    {
        let ta_name = "CDLTASUKIGAP";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_tasukigap_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_tasukigap_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_tasukigap_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_tasukigap", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_tasukigap".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_thrusting (TA::CDLTHRUSTING) ---
    {
        let ta_name = "CDLTHRUSTING";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_thrusting_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_thrusting_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_thrusting_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_thrusting", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_thrusting".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_tristar (TA::CDLTRISTAR) ---
    {
        let ta_name = "CDLTRISTAR";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_tristar_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_tristar_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_tristar_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_tristar", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_tristar".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_unique3river (TA::CDLUNIQUE3RIVER) ---
    {
        let ta_name = "CDLUNIQUE3RIVER";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_unique3river_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_unique3river_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_unique3river_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_unique3river", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_unique3river".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_upsidegap2crows (TA::CDLUPSIDEGAP2CROWS) ---
    {
        let ta_name = "CDLUPSIDEGAP2CROWS";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_upsidegap2crows_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_upsidegap2crows_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_upsidegap2crows_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_upsidegap2crows", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_upsidegap2crows".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }

    // === cdl_xsidegap3methods (TA::CDLXSIDEGAP3METHODS) ---
    {
        let ta_name = "CDLXSIDEGAP3METHODS";
        let (group, opt_vals) = talib_ffi::get_func_meta(ta_name)
            .unwrap_or_else(|| (String::new(), Vec::new()));
        let mut out = vec![0.0f64; N];
        adaq_talib::pattern::cdl_xsidegap3methods_with_output(&open, &high, &low, &close, &mut out).unwrap(); // warmup
        let _t0 = Instant::now();
        for _ in 0..3 { adaq_talib::pattern::cdl_xsidegap3methods_with_output(&open, &high, &low, &close, &mut out).unwrap(); }
        let _per = _t0.elapsed().as_nanos().max(1);
        let iters = ((BUDGET_NS / _per) as usize).clamp(10, 400);
        let _start = Instant::now();
        let mut _ack = 0.0f64;
        for _ in 0..iters { adaq_talib::pattern::cdl_xsidegap3methods_with_output(&open, &high, &low, &close, &mut out).unwrap(); _ack += out[N - 1]; }
        let adaq_ns = _start.elapsed().as_nanos() as f64 / (iters as f64 * N as f64);
        let (_cns, _cck, _cmiss) = if ta_name.starts_with("CDL") {
            match talib_ffi::c_candle(ta_name, N, &open, &high, &low, &close, iters) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        } else {
            match talib_ffi::c_abstract(ta_name, N, &real0, &real1, &open, &high, &low, &close, &vol, iters, &opt_vals) {
                Some((a, b)) => (a, b, false),
                None => (0.0, 0.0, true),
            }
        };
        if !_cmiss && (_ack - _cck).abs() > 1e-3 {
            eprintln!("PARITYDEBUG {} adaq={:.6} c={:.6} adaq_ns={:.4} c_ns={:.4}", "cdl_xsidegap3methods", _ack, _cck, adaq_ns, _cns);
        }
        rows.push(BenchRow {
            name: "cdl_xsidegap3methods".to_string(),
            group,
            adaq_ns,
            c_ns: if _cmiss { 0.0 } else { _cns },
            parity: if _cmiss { f64::NAN } else { (_ack - _cck).abs() },
            c_missing: _cmiss,
        });
    }
    unsafe { talib_ffi::TA_Shutdown(); }
    rows
}

#[cfg(feature = "bench-c")]
fn main() {
    let rows = run_all();
    let mut csv = String::from("name,group,adaq_ns_per_elem,c_ns_per_elem,speedup,parity,c_missing\n");
    let mut n_total = 0;
    let mut n_missing = 0;
    let mut n_parity_bad = 0;
    let mut sum_ratio_log = 0.0f64;
    let mut n_compared = 0;
    for r in &rows {
        n_total += 1;
        let speedup = if r.c_missing || r.c_ns == 0.0 { f64::NAN } else { r.c_ns / r.adaq_ns };
        let parity_str = if r.c_missing { "NA".to_string() } else if r.parity.is_nan() { "NA".to_string() } else { format!("{:.3e}", r.parity) };
        csv.push_str(&format!("{},{},{:.4},{:.4},{},{},{}\n",
            r.name, r.group, r.adaq_ns, r.c_ns,
            if speedup.is_nan() { "NA".to_string() } else { format!("{:.3}", speedup) },
            parity_str, r.c_missing));
        if r.c_missing { n_missing += 1; } else {
            n_compared += 1;
            sum_ratio_log += speedup.ln();
            if !r.parity.is_nan() && r.parity > 1e-6 { n_parity_bad += 1; }
        }
    }
    println!("{}", csv);
    println!("SUMMARY total={} compared={} c_missing={} parity_bad(>1e-6)={}", n_total, n_compared, n_missing, n_parity_bad);
    if n_compared > 0 {
        let geo = (sum_ratio_log / n_compared as f64).exp();
        println!("SUMMARY geomean_speedup(adaq_vs_C)={:.3}x", geo);
    }
    let _ = std::fs::write("all161_results.csv", &csv);
}

#[cfg(not(feature = "bench-c"))]
fn main() {
    println!("Enable with --features bench-c");
}
